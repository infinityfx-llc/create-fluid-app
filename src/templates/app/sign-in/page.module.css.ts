export default function signInCss() {
    return `.container {
    display: flex;
    align-items: center;
    justify-content: center;
    padding-block: var(--f-spacing-med);
    flex-grow: 1;
}`;
}